# 图片约定

把截图放进本目录，文件名与下表一致，README 里的占位符就会自动显示。

| 文件名 | 内容 |
|--------|------|
| `cost-panel.png` | 对话界面 tab 栏里「成本统计」展开后的面板，能看到按模型分组、token 用量、费用、缓存命中率 |
| `cost-analysis.png` | 全局「成本分析」视图：顶部总成本 / 请求数 / token 的 KPI，下方按模型、按资产的成本分布与占比 |

建议：宽 1200px 左右，PNG 格式，单张 500KB 以内。截图里别带账号 / token 等敏感信息。
